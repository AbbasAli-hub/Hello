name: Multiple Jobs Workflow

on:
  push:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: |
          echo "Building Application..."
          exit 1

  job2:
    needs: job1
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Testing failures"
